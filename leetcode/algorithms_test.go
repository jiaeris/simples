package leetcode

import (
	"fmt"
	"testing"
)

func Test_MyTest(t *testing.T) {
	//result := hammingDistance(1, 4)
	//result := judgeCircle("UULLDDRR")
	//result := arrayPairSum([]int{1, 4, 3, 2})
	//result := findComplement(5)
	//result := findWords([]string{"Hello", "Alaska", "Dad", "Peace"})
	//result := reverseWords("s'teL ekat edoCteeL tsetnoc")
	result := reverseString("a canal")
	fmt.Println(result)
}
